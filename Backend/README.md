# Ethereal Backend

## Setup

1. Create `.env` (already included, update values)
2. Install deps
3. Run server

## Scripts

- `npm run dev`
- `npm start`

## Environment variables

- `PORT`
- `MONGODB_URI`
- `JWT_SECRET`
- `CORS_ORIGIN`
